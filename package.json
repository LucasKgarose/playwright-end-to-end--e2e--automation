{
  "name": "playwright-end-to-end-(e2e)-automation",
  "version": "1.0.0",
  "description": "* install PLaywright\r * Automate adding 3 todos, mark 1 as completed, delete,\r * Generate Playwright *HTML report* and open it\r * Add assertions and check failure in report\r * Take screenshots and video on failure and attach to report\r * Run with inteactive UI\r * Run in debug mode\r * Run with trace on",
  "main": "index.js",
  "scripts": {},
  "keywords": [],
  "author": "Lucas Kgarose",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "playwright": "^1.59.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0"
  }
}
